% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validate}
\alias{validate}
\alias{validate.default}
\alias{validate.ast}
\alias{validate.dims}
\alias{validate.set}
\alias{validate.symbol}
\alias{validate.mapping}
\alias{validate.ast_formula}
\alias{validate.parameter}
\alias{validate.variable}
\alias{validate.equation}
\alias{validate.model}
\title{Validate multimod or AST objects}
\usage{
validate(x, context = NULL, stop_on_error = TRUE, recursive = TRUE, ...)

\method{validate}{default}(x, context = NULL, stop_on_error = TRUE, recursive = TRUE, ...)

\method{validate}{ast}(x, context = NULL, stop_on_error = TRUE, recursive = TRUE, ...)

\method{validate}{dims}(x, context = NULL, stop_on_error = TRUE, recursive = TRUE, ...)

\method{validate}{set}(x, context = NULL, stop_on_error = TRUE, recursive = TRUE, ...)

\method{validate}{symbol}(x, context = NULL, stop_on_error = TRUE, recursive = TRUE, ...)

\method{validate}{mapping}(x, context = NULL, stop_on_error = TRUE, recursive = TRUE, ...)

\method{validate}{ast_formula}(x, context = NULL, stop_on_error = TRUE, recursive = TRUE, ...)

\method{validate}{parameter}(x, context = NULL, stop_on_error = TRUE, recursive = TRUE, ...)

\method{validate}{variable}(x, context = NULL, stop_on_error = TRUE, recursive = TRUE, ...)

\method{validate}{equation}(x, context = NULL, stop_on_error = TRUE, recursive = TRUE, ...)

\method{validate}{model}(x, verbose = FALSE, stop_on_error = TRUE, ...)
}
\arguments{
\item{x}{A multimod model object}

\item{context}{Optional character label identifying the object (used in
error messages for AST or component validators)}

\item{stop_on_error}{Logical; if TRUE, stop with an error if validation fails.
Default is TRUE.}

\item{recursive}{Logical; when TRUE (default) validators recursively inspect
known child AST objects (e.g., dims, formulas). Set to FALSE to limit
validation to the top-level object only (ignored for model validation).}

\item{...}{Additional arguments (currently unused)}

\item{verbose}{Logical; if TRUE, print detailed progress messages for all checks.
Default is FALSE.}
}
\value{
A list containing \code{valid}, \code{errors}, \code{warnings}, \code{info}, and \code{context}
when \code{stop_on_error = FALSE}. Invisibly returns the same result when
\code{stop_on_error = TRUE} and validation passes.

A list with components:
\item{valid}{Logical indicating if model passed all checks}
\item{errors}{Character vector of error messages (critical issues)}
\item{warnings}{Character vector of warning messages (potential issues)}
\item{info}{Character vector of informational messages}
}
\description{
Performs structural validation on low-level AST nodes and multimod objects
(parameters, variables, equations, etc.) as well as whole model objects.
These helpers are intended to catch malformed objects early (e.g.,
dimensions stored as raw character vectors instead of \code{ast_dims}). Call with
\code{stop_on_error = FALSE} to collect diagnostics without raising errors.
}
\details{
Current checks include:
\itemize{
\item Presence of objective function
\item Collision detection between set aliases and iterator variable names
\item Verification that base sets referenced by aliases exist
\item Detection of potential ambiguous naming
}
}
\section{Methods (by class)}{
\itemize{
\item \code{validate(model)}: Validate a multimod model

Checks a multimod model for structural issues, naming conflicts, and other
potential problems that could cause issues during export or solving.

}}
\examples{
\dontrun{
m <- read_gmpl("model.mod", data_file = "data.dat")
validation <- validate(m)
if (!validation$valid) {
  cat("Errors found:\n")
  cat(paste(validation$errors, collapse = "\n"))
}
}

}
