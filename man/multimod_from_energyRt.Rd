% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/energyrt_pipeline.R
\name{multimod_from_energyRt}
\alias{multimod_from_energyRt}
\title{Build a multimod model from an interpolated energyRt scenario}
\usage{
multimod_from_energyRt(
  scen,
  gms = NULL,
  prefer = c("spec", "hint"),
  inMemory = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{scen}{An interpolated energyRt scenario.}

\item{gms}{Path to \code{energyRt.gms}. Defaults to the copy shipped with the
installed energyRt; pass a path to read a working tree instead.}

\item{prefer}{Passed to \code{\link[=fill_variable_domains]{fill_variable_domains()}}.}

\item{inMemory}{Load the scenario's data into memory. Must be \code{TRUE} for a
folded scenario (the lazy path cannot expand wildcards) - see
\code{\link[=import_energyRt_data]{import_energyRt_data()}}.}

\item{verbose}{Logical; report progress.}
}
\value{
A multimod model with data attached, ready for \code{\link[=model_to_lp]{model_to_lp()}}.
}
\description{
Structure is read from energyRt's GAMS template; data comes from the
scenario. This is the entry point for the direct-matrix / MPS route -
everything downstream (\code{\link[=model_to_lp]{model_to_lp()}}, \code{\link[=write_mps]{write_mps()}}, \code{\link[=solve_highs]{solve_highs()}})
takes the model this returns.
}
\examples{
\dontrun{
scen <- energyRt::interpolate_model(mod, name = "BASE")
m <- multimod_from_energyRt(scen)
lp <- model_to_lp(m)
write_mps(m, "model.mps", lp = lp)
}
}
