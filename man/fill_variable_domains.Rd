% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/energyrt_pipeline.R
\name{fill_variable_domains}
\alias{fill_variable_domains}
\title{Fill in variable domains that the GAMS comments do not carry}
\usage{
fill_variable_domains(
  model,
  prefer = c("spec", "hint"),
  scalars = "vObjective",
  verbose = TRUE
)
}
\arguments{
\item{model}{A multimod model.}

\item{prefer}{Where the hint and the spec disagree: \code{"spec"} trusts
\code{.mapping_spec}, \code{"hint"} keeps the GAMS comment. Three variables differ
(\code{vTechStockCap}, \code{vStorageInp}, \code{vStorageOut}). \code{"spec"} is the default
because it is the choice that reproduces GLPK's column count (2,318 on the
R1 kit, against 2,295 for \code{"hint"}) - the three hints are stale.}

\item{scalars}{Variables that legitimately have no domain.}

\item{verbose}{Logical; report what was filled and what conflicted.}
}
\value{
The model, with every variable carrying a domain.
}
\description{
\code{en_extract_domains_from_comments()} reads \verb{*@ mXxx(...)} hints out of the
GAMS template. As of energyRt 0.89.5 there are hints for 70 of 93 variables,
so the rest (phase-out / retirement / storage auxiliary capacity) would get
no domain. energyRt's own \code{.mapping_spec} carries the authoritative table and
covers 92 of 93 (\code{vObjective} is a genuine scalar).
}
