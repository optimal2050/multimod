% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup_python.R
\name{setup_python_environment}
\alias{setup_python_environment}
\alias{setup_multimod_python}
\title{Configure a Python environment for multimod}
\usage{
setup_python_environment(
  env_name = "multimod",
  packages = c("pyomo", "highspy"),
  python_version = NULL,
  ensure_miniconda = TRUE,
  reinstall = FALSE,
  pip = TRUE,
  ...
)

setup_multimod_python(
  env_name = "multimod",
  packages = c("pyomo", "highspy"),
  python_version = NULL,
  ensure_miniconda = TRUE,
  reinstall = FALSE,
  pip = TRUE,
  ...
)
}
\arguments{
\item{env_name}{Name of the Conda environment to manage. Defaults to
"multimod" so the generated Pyomo artifacts have a predictable runtime.}

\item{packages}{Character vector of Python packages to install once the
environment exists. Defaults to both \code{"pyomo"} and \code{"highspy"} so a
usable solver backend is available immediately. Set to \code{NULL} or
\code{character(0)} to skip installation.}

\item{python_version}{Optional Python version spec passed to
\code{\link[reticulate:conda-tools]{reticulate::conda_create()}}. If \code{NULL}, reticulate's default is used.}

\item{ensure_miniconda}{Logical flag controlling whether
\code{\link[reticulate:install_miniconda]{reticulate::install_miniconda()}} should run when no Conda binary is
detected on the system. Defaults to \code{TRUE} to minimize user setup.}

\item{reinstall}{Logical flag; when \code{TRUE} the environment is recreated even
if it already exists. Defaults to \code{FALSE} to preserve prior installs.}

\item{pip}{Logical; when \code{TRUE} (default) packages are installed via
\code{pip} inside the Conda environment (ensures \code{highspy} is available).
Set to \code{FALSE} to install via conda instead.}

\item{...}{Additional arguments forwarded to
\code{\link[reticulate:install_miniconda]{reticulate::install_miniconda()}} when \code{ensure_miniconda = TRUE}.}
}
\value{
Invisibly returns the environment name once configured.
}
\description{
This helper bootstraps a Conda environment (named "multimod" by default)
using the \href{https://rstudio.github.io/reticulate/}{reticulate} toolkit. It
ensures \code{reticulate::install_miniconda()} has been invoked (unless disabled),
creates the target environment when missing, installs the requested Python
packages, and activates the environment for the current R session via
\code{reticulate::use_condaenv()}.
}
\details{
After setup, you can optionally configure multimod to use this environment
via \code{set_multimod_python()} or by letting \code{get_multimod_python()} detect it
automatically through reticulate.
}
\seealso{
\code{\link[=get_multimod_python]{get_multimod_python()}}, \code{\link[=set_multimod_python]{set_multimod_python()}}
}
