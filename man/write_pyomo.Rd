% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_pyomo.R
\name{write_pyomo}
\alias{write_pyomo}
\title{Write Pyomo model artifacts}
\usage{
write_pyomo(
  model,
  file = NULL,
  model_dir = NULL,
  solver_dir = "pyomo",
  model_name = "model",
  solver = "highs",
  use_folded = TRUE,
  cleanup = TRUE,
  data_mode = c("external", "embedded"),
  split = FALSE,
  export_vars = FALSE,
  export_data = FALSE,
  export_mps = FALSE,
  ...
)
}
\arguments{
\item{model}{A \code{multimod} model object}

\item{file}{Output file path (optional, overrides model_dir/solver_dir)}

\item{model_dir}{Directory where model was saved with save_model() (contains model.rds)}

\item{solver_dir}{Solver subdirectory name (default: "jump", e.g., "jump-highs", "jump-cplex")}

\item{model_name}{Name for the JuMP model variable (default: "model")}

\item{solver}{Python solver name passed to \code{pyomo.opt.SolverFactory}
(default \code{"highs"}).}

\item{use_folded}{logical; whether to use folded equations if available (default: TRUE)}

\item{cleanup}{logical; whether to remove existing solver_dir before generating new files (default: TRUE)}

\item{data_mode}{Whether data should be stored externally in files
(\code{"external"}, default) or embedded directly inside the generated
Python code (\code{"embedded"}).}

\item{split}{Logical flag controlling whether solver execution should live
in a separate \code{solve.py} (when \code{TRUE}) or be embedded directly in
\code{model.py} (default). The embedded mode mirrors the two-file workflow used
by the other writers (\code{model} + \code{data}). Set \code{split = TRUE} to preserve the
previous three-file scaffold.}

\item{export_vars}{logical; whether to export variable values to CSV files in vars_export/ directory after solve (default: FALSE).
Note: Solution is always saved to solution/ directory regardless of this setting (required for load_results).}

\item{export_data}{logical; whether to export parameter data to CSV files in data_export/ directory (default: FALSE)}

\item{export_mps}{logical or character; if TRUE, exports model to MPS format; if character, specifies filename (default: FALSE)}

\item{...}{Additional arguments (not used)}
}
\description{
This mirrors \code{write_jump()} but targets Python/Pyomo. The function emits a
\code{model.py} file containing component declarations, a \code{data_loader.py} helper
that knows how to stream data from the saved model directory (or use
embedded literals), and a lightweight \code{solve.py} runner.
}
