% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_storage.R
\name{.generate_model_metadata_csv}
\alias{.generate_model_metadata_csv}
\title{Save multimod model workspace to disk}
\usage{
.generate_model_metadata_csv(model, model_dir)
}
\arguments{
\item{model}{A multimod model object}

\item{path}{Character. Directory path for model workspace}

\item{format}{Character. Data format: "csv" (default), "ipc" (Arrow IPC/Feather), or "parquet"}

\item{compression}{Character. Compression codec for Arrow formats: "zstd" (default), "lz4", "snappy", or "uncompressed"}

\item{compression_level}{Integer. Compression level (default: 15 for zstd)}

\item{overwrite}{Logical. Overwrite existing model workspace directory}

\item{keep_in_memory}{Logical. Keep data in memory after saving (default: TRUE)}

\item{save_structure}{Logical. Save equations, variables, and other structure (default: TRUE)}

\item{verbose}{Logical. Print progress messages}
}
\value{
Modified model object with storage metadata
}
\description{
Save a complete multimod model to a workspace directory with efficient Arrow storage for
data frames. The directory structure mirrors the model object structure
for consistency and easy navigation.
}
\details{
Directory structure created:
\preformatted{
model_path/
  model.rds              # Model structure (thinned if keep_in_memory=FALSE)
  metadata.json          # Human-readable metadata
  format.txt             # Storage format
  sets/
    set_name.txt         # Set members (one per line)
  parameters/
    param_name/
      data.{csv|arrow|parquet}   # Parameter data
      metadata.rds               # Parameter metadata
  mappings/
    mapping_name/
      data.{csv|arrow|parquet}
  folded_data/           # If folding applied
    param_name/
      folded_data.{csv|arrow|parquet}
  equations/
    equations.rds        # All equations
  variables/
    variables.rds        # All variables
}
}
\examples{
\dontrun{
# Save with default CSV format
model <- save_model(model, "models/utopia")

# Save with Arrow IPC (fastest)
model <- save_model(model, "models/utopia", format = "ipc")

# Save and free memory
model <- save_model(model, "models/utopia",
                    format = "ipc",
                    keep_in_memory = FALSE)
}

}
\keyword{internal}
