% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_solution_mps.R
\name{read_cuopt_solution}
\alias{read_cuopt_solution}
\title{Parse a cuOpt solution file}
\usage{
read_cuopt_solution(file, require_optimal = TRUE)
}
\arguments{
\item{file}{Path to the solution file; \code{.gz} is read transparently.}

\item{require_optimal}{Logical; error unless the status is optimal. A capped
or interrupted solve returns a feasible but sub-optimal primal, and
presenting that as the optimum is the failure this guards.}
}
\value{
The same shape as \code{\link[=read_highs_solution]{read_highs_solution()}}: \code{status}, \code{objective},
\code{columns} (\code{name}, \code{primal}, \code{dual} = \code{NA}), and \code{rows} = \code{NULL} to mark
the absence of dual information.
}
\description{
cuOpt writes a different shape from HiGHS: two comment lines carrying the
status and the objective, then one \verb{name value} pair per column.
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{# Status: Optimal
# Objective value: 36880.2354212843493
c0 0
c1 1.5
}\if{html}{\out{</div>}}

There is \strong{no row section}: cuOpt's \code{--solution-file} writes primal values
only, so nothing on this path yields duals or marginals.
}
