% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_redundancy.R
\name{analyze_parameter_redundancy}
\alias{analyze_parameter_redundancy}
\title{Analyze parameter redundancy in model}
\usage{
analyze_parameter_redundancy(
  model,
  params = NULL,
  verbose = TRUE,
  return_details = FALSE
)
}
\arguments{
\item{model}{A multimod model object (folded or unfolded)}

\item{params}{Character vector of parameter names to analyze. If NULL (default),
analyzes all parameters with data.}

\item{verbose}{Logical; print detailed output (default: TRUE)}

\item{return_details}{Logical; return detailed data frame instead of summary (default: FALSE)}
}
\value{
If return_details=FALSE, invisibly returns summary data frame.
If return_details=TRUE, returns detailed data frame with per-parameter statistics.
}
\description{
Reports repeated values in parameters to identify redundancy before and after folding.
This helps assess folding effectiveness and identify remaining optimization potential.
}
\details{
For each parameter with data, calculates:
\itemize{
\item Original redundancy: ratio of repeated values to total rows
\item Folded redundancy: ratio of repeated values in folded data (if folded)
\item Compression achieved through folding
}

Redundancy metrics:
\itemize{
\item \strong{Original redundancy}: percentage of rows that have duplicate values
(within the full parameter dataset)
\item \strong{Folded redundancy}: percentage of rows with duplicate values after folding
\item \strong{Compression ratio}: original rows / folded rows
\item \strong{Remaining potential}: estimated further compression if all redundancy removed
}

High original redundancy with no folding indicates missed optimization opportunity.
High folded redundancy suggests additional dimensions could be folded.
}
\examples{
\dontrun{
# Analyze all parameters
analyze_parameter_redundancy(model)

# Analyze specific parameters
analyze_parameter_redundancy(model, params = c("pTechCinp2use", "pSupCost"))

# Get detailed results
results <- analyze_parameter_redundancy(model, return_details = TRUE)
View(results)
}

}
