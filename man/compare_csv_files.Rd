% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_dataframes.R
\name{compare_csv_files}
\alias{compare_csv_files}
\title{Compare CSV files in two directories}
\usage{
compare_csv_files(
  path1,
  path2,
  pattern = "\\\\.csv$",
  tolerance = 1e-09,
  ignore.case = FALSE,
  key_col = NULL,
  ...
)
}
\arguments{
\item{path1}{Path to first directory}

\item{path2}{Path to second directory}

\item{pattern}{Regular expression pattern to filter CSV files (default: "\\.csv$")}

\item{tolerance}{Numeric tolerance for comparing numeric columns (default: 1e-9)}

\item{ignore.case}{Logical; if TRUE, perform case-insensitive comparison (default: FALSE)}

\item{key_col}{Column name(s) to use as key (default: NULL = auto-detect). If NULL, uses all columns except "value"}

\item{...}{Additional arguments passed to compare_dataframes}
}
\value{
List with components:
\itemize{
\item only_in_path1: Files only in path1
\item only_in_path2: Files only in path2
\item in_both: Files in both directories
\item comparisons: Named list of comparison results for each file showing differences
\item identical_files: Files that were compared and identical
\item different_files: Files that were compared and had differences
}
}
\description{
Finds CSV files with matching names in two directories and compares them.
Reports files that are only in one directory or the other, and shows
differences for files that exist in both.
}
