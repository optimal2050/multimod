% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/highs_solve.R
\name{write_mps}
\alias{write_mps}
\title{Write a model to MPS, with the index tables needed to read results back}
\usage{
write_mps(
  model,
  file,
  index_dir = dirname(file),
  lp = NULL,
  format = c("parquet", "ipc", "csv"),
  verbose = FALSE
)
}
\arguments{
\item{model}{A multimod model with data attached.}

\item{file}{Path of the \code{.mps} file to write.}

\item{index_dir}{Directory for the index tables; defaults to the directory
of \code{file}.}

\item{lp}{Optional pre-built LP from \code{\link[=model_to_lp]{model_to_lp()}}.}

\item{format}{Storage format for the index tables, passed to the package's
\code{.save_dataframe()}: \code{"parquet"} (default), \code{"ipc"} or \code{"csv"}.}

\item{verbose}{Logical; report progress.}
}
\value{
Invisibly, a list of the paths written.
}
\description{
HiGHS writes MPS quickly precisely because it does not carry symbolic names:
the file uses positional ones (\code{c0}, \code{r0}, ...). The row and column index
tables written alongside carry the mapping back to \verb{(symbol, index tuple)},
so a solution vector returned by a remote solver can be interpreted.
}
\details{
\strong{The \code{.mps} and its index files must travel together} - the file alone
cannot be mapped back to model symbols.
}
