% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ast-classes.R
\name{ast_shift}
\alias{ast_shift}
\title{Create an index shift AST node for ordered set references}
\usage{
ast_shift(symbol, offset, ...)
}
\arguments{
\item{symbol}{A character string representing the index symbol (e.g., "y", "ls", "ld").}

\item{offset}{An integer offset. Negative for backward shift (e.g., -1 for y-1), positive for forward shift (e.g., +1 for y+1).}
}
\value{
A \code{shift} S3 object (subclass of \code{ast}).
}
\description{
Constructs a shifted index reference for ordered sets (e.g., YEAR, SEASON).
Used for GAMS lag/lead operators like \code{y-1} (previous year) or \code{y+1} (next year).
Negative offset represents backward shift (lag), positive represents forward shift (lead).
}
\examples{
ast_shift("y", -1)  # y-1 (previous year)
ast_shift("y", 1)   # y+1 (next year)
ast_shift("ls", -1) # ls-1 (previous season)
}
