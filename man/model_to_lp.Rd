% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/highs_solve.R
\name{model_to_lp}
\alias{model_to_lp}
\title{Assemble a model into LP arrays}
\usage{
model_to_lp(
  model,
  col_index = NULL,
  row_index = NULL,
  on_duplicate = c("error", "last", "first"),
  verbose = FALSE
)
}
\arguments{
\item{model}{A multimod model with data attached.}

\item{col_index, row_index}{Optional index tables; built when not supplied.}

\item{on_duplicate}{Passed to \code{\link[=build_triplets]{build_triplets()}}; how to resolve a parameter
that stores one index tuple with several distinct values.}

\item{verbose}{Logical; report progress.}
}
\value{
A list with
\code{A} (a \code{Matrix::dgCMatrix}), \code{obj}, \code{row_lo}, \code{row_up}, \code{col_lo}, \code{col_up},
\code{maximum}, and the \code{row_index} / \code{col_index} used. Row bounds encode the
constraint sense: \code{==} gives equal bounds, \code{<=} an upper bound only, \code{>=}
a lower bound only.
}
\description{
Assemble a model into LP arrays
}
