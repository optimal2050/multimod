% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_gmpl.R
\name{as_gmpl_ord_card}
\alias{as_gmpl_ord_card}
\title{Render GAMS \code{ord()} / \code{card()} for GMPL}
\usage{
as_gmpl_ord_card(x, model = NULL, ...)
}
\arguments{
\item{x}{A \code{func} AST node named \code{ord} or \code{card}.}

\item{model}{The model, used to resolve set aliases and to check that the
position parameter actually exists.}

\item{...}{Additional arguments passed to \code{as_gmpl}.}
}
\value{
A GMPL parameter reference, e.g. \code{ordYear[y]}.
}
\description{
GAMS \code{ord(s)} is the 1-based position of a member within its set, and
\code{card(s)} the set's cardinality. \strong{MathProg has neither function.} The only
faithful translation is a parameter carrying those values, which the model
must supply - substituting the member's own value is wrong whenever set
members are not consecutive integers (e.g. years 2030, 2040, 2050), and
\code{ord(y) < pOlife + ord(yp)} is position arithmetic, not value arithmetic.
}
\details{
The expected parameter follows the convention \verb{ord<Set>} / \verb{card<Set>} on the
\emph{base} set, so \code{ord(year)}, \code{ord(yearp)} and \code{ord(yeare)} all render as
\verb{ordYear[<iterator>]}. Alias sets are resolved to their base before naming.
}
\keyword{internal}
