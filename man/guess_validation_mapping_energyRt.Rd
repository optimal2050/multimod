% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fold.R
\name{guess_validation_mapping_energyRt}
\alias{guess_validation_mapping_energyRt}
\title{Guess validation mapping for fold specification (energyRt-specific)}
\usage{
guess_validation_mapping_energyRt(
  param_name,
  dim_to_fold,
  dims,
  entity_mapping
)
}
\arguments{
\item{param_name}{Parameter name (for debugging)}

\item{dim_to_fold}{Dimension to fold}

\item{dims}{All dimensions in parameter}

\item{entity_mapping}{Named list mapping entity dimensions to validation mappings.
Values can be single string or character vector for chains.
Example: list(tech = "mTechTimeslice",
stg = c("mStorageComm", "mCommTimeslice"))}
}
\value{
Mapping specification: single string or character vector for chains
}
\description{
Uses parameter dimensions and entity-to-mapping configuration to select
the appropriate validation mapping. Uses PRIORITY-BASED selection: returns
the mapping for the FIRST matching entity dimension found.
}
\details{
Supports mapping chains for derived mappings (e.g., stg → comm → timeslice).
}
\keyword{internal}
