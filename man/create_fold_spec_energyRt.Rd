% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fold.R
\name{create_fold_spec_energyRt}
\alias{create_fold_spec_energyRt}
\title{Create fold specification for energyRt models (convenience wrapper)}
\usage{
create_fold_spec_energyRt(model, fold_dims, tolerance = 1e-10, verbose = TRUE)
}
\arguments{
\item{model}{A multimod model object (from energyRt)}

\item{fold_dims}{Dimension configuration. Same format as \code{create_fold_spec()}.
Can be:
\itemize{
\item Character vector: dimension names (e.g., \code{c("timeslice")})
\item Named list (simple): dimension → mapping (e.g., \code{list(timeslice = "mCommTimeslice")})
\item Named list (advanced): dimension → entity-specific mappings
(e.g., \code{list(timeslice = list(tech = "mTechTimeslice", comm = "mCommTimeslice"))})
}}

\item{tolerance}{Numeric tolerance for considering values equal (default: 1e-10)}

\item{verbose}{Logical; print progress messages (default: TRUE)}
}
\value{
Data frame with columns: param_name, dim_to_fold, validation_mapping,
can_fold, reason
}
\description{
Convenience wrapper around the generic \code{create_fold_spec()} function.
Uses the same underlying implementation but provides energyRt-specific defaults
for entity-to-mapping associations.
}
\details{
\strong{Note}: This function has the SAME functionality as \code{create_fold_spec()}.
The only difference is naming - this version emphasizes it's designed for energyRt
models, but both functions support all features (entity-specific mappings, chains, etc.).

Mapping priority (for entity-specific configuration):
\itemize{
\item Uses FIRST matching entity dimension
\item If parameter has "tech" → use tech mapping
\item Else if has "sup" → use sup mapping
\item Else if has "comm" → use comm mapping
\item Else if has "stg" → use stg mapping
\item Else → NA (no validation)
}

Mapping chains join mappings by common columns:
Example: \code{stg = c("mStorageComm", "mCommTimeslice")}
\itemize{
\item \code{mStorageComm[stg, comm]} ⨝ \code{mCommTimeslice[comm, timeslice]} → \code{[stg, timeslice]}
}

For derived mappings (chains), mappings are joined in sequence by common columns.
Example: \code{stg = c("mStorageComm", "mCommTimeslice")}
\itemize{
\item \code{mStorageComm[stg, comm]} inner_join \code{mCommTimeslice[comm, timeslice]} → \code{[stg, timeslice]}
}
}
\examples{
\dontrun{
# Explicit configuration for energyRt models
fold_spec <- create_fold_spec_energyRt(model, 
  fold_dims = list(
    timeslice = list(
      tech = "mTechTimeslice",
      sup = "mSupTimeslice", 
      comm = "mCommTimeslice",
      stg = c("mStorageComm", "mCommTimeslice"),
      trade = c("mTradeComm", "mCommTimeslice"),
      imp = c("mImportComm", "mCommTimeslice")
    )
  ))

# Review and edit
View(fold_spec)

# Use edited specification
model <- fold_model(model, fold_spec = fold_spec)
}
}
