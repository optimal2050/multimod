% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_matrix.R
\name{build_triplets}
\alias{build_triplets}
\title{Extract (row, column, coefficient) triplets from a model}
\usage{
build_triplets(
  model,
  col_index = NULL,
  row_index = NULL,
  equations = NULL,
  on_duplicate = c("error", "last", "first"),
  verbose = FALSE
)
}
\arguments{
\item{model}{A multimod model with data attached.}

\item{col_index, row_index}{Index tables from \code{\link[=build_col_index]{build_col_index()}} /
\code{\link[=build_row_index]{build_row_index()}}. Built on demand when not supplied.}

\item{equations}{Optional character vector limiting which equations to
process; useful when bringing the backend up equation by equation.}

\item{on_duplicate}{What to do when a parameter stores the same index tuple
with more than one distinct value. \code{"error"} (default) refuses, because the
lookup is genuinely ambiguous; \code{"last"} takes the last, matching what GAMS
and Pyomo do silently; \code{"first"} takes the first. Either non-default choice
reports the parameter and the number of tuples affected.}

\item{verbose}{Logical; report per-equation term counts and timings.}
}
\value{
A list with \code{triplets} (\code{data.table(i, j, coef)}, aggregated), \code{rhs}
(\code{data.table(i, rhs)}), and \code{row_index} / \code{col_index} as used.
}
\description{
Walks each equation's AST set-at-a-time and returns the LP matrix entries
together with the right-hand side implied by the equation's constant terms.
}
