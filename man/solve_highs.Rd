% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/highs_solve.R
\name{solve_highs}
\alias{solve_highs}
\title{Solve a model in-process with HiGHS}
\usage{
solve_highs(model, lp = NULL, attach = TRUE, control = list(), verbose = FALSE)
}
\arguments{
\item{model}{A multimod model with data attached.}

\item{lp}{Optional pre-built LP from \code{\link[=model_to_lp]{model_to_lp()}}.}

\item{attach}{Logical; attach primal values back onto \code{model$variables} as a
long data.frame with named index columns, matching what
\code{read_csv_results()} produces so downstream readers keep working.}

\item{control}{Optional list of HiGHS options.}

\item{verbose}{Logical; report progress.}
}
\value{
A list with \code{objective}, \code{status}, \code{status_message}, \code{primal}
(a \code{data.table} of \code{j}, symbol, index tuple and value), \code{dual} (per row),
the \code{lp}, and - when \code{attach} is \code{TRUE} - the updated \code{model}.
}
\description{
Uses the solver-object interface rather than \code{\link[highs:highs_solve]{highs::highs_solve()}}, because
only that path returns dual values - energy-system models generally need the
constraint marginals, not just primal levels.
}
