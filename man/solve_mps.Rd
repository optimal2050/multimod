% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/highs_solve.R
\name{solve_mps}
\alias{solve_mps}
\title{Solve a written MPS file locally and emit a HiGHS solution file}
\usage{
solve_mps(
  file,
  sol_file = NULL,
  control = list(),
  require_optimal = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{file}{Path to the MPS file (uncompressed; the HiGHS reader does not
accept \code{.gz}).}

\item{sol_file}{Path for the solution file; default \code{file} with a \code{.sol}
extension.}

\item{control}{Optional list of HiGHS options (e.g.
\code{list(solver = "ipm", run_crossover = "on")}).}

\item{require_optimal}{Logical; error unless the run ends optimal.}

\item{verbose}{Logical; report progress.}
}
\value{
A list with \code{status_message}, \code{objective}, \code{sol_file} and
\code{run_time} (seconds).
}
\description{
The fully local counterpart of the cloud solve leg: reads an MPS file
written by \code{\link[=write_mps]{write_mps()}}, solves it in-process with HiGHS, and writes a
raw-style solution file that \code{\link[=read_highs_solution]{read_highs_solution()}} /
\code{\link[=read_mps_solution]{read_mps_solution()}} consume unchanged. Positional column/row names
(\code{c0}, \code{r0}, ...) are reconstructed from the solver dimensions, matching
what \code{\link[=write_mps]{write_mps()}} emits; files with other naming conventions decode
correctly only through their own index tables.
}
