% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_solution_mps.R
\name{read_mps_solution}
\alias{read_mps_solution}
\title{Map a solved MPS back onto model symbols}
\usage{
read_mps_solution(
  sol,
  index_dir = NULL,
  col_index = NULL,
  row_index = NULL,
  base = NULL,
  format = c("parquet", "ipc", "csv"),
  solver = c("auto", "highs", "cuopt"),
  require_optimal = TRUE
)
}
\arguments{
\item{sol}{Path to a HiGHS solution file, or the list returned by
\code{\link[=read_highs_solution]{read_highs_solution()}}.}

\item{index_dir}{Directory holding the \verb{*_col_index} / \verb{*_row_index} tables
written by \code{\link[=write_mps]{write_mps()}}. Ignored when both index tables are supplied.}

\item{col_index, row_index}{Index tables, if already in memory.}

\item{base}{Basename used when the index files were written; inferred from
the directory when there is exactly one candidate.}

\item{format}{Storage format of the index tables.}

\item{solver}{Which solver wrote \code{sol}; \code{"auto"} sniffs the file.}

\item{require_optimal}{Passed to the reader.}
}
\value{
A list with \code{objective}, \code{status}, \code{primal} (\code{j}, symbol, name,
value) and \code{dual} (\code{i}, symbol, name, value, marginal) - the same shape
\code{\link[=solve_highs]{solve_highs()}} returns, so the two paths are interchangeable.
}
\description{
Map a solved MPS back onto model symbols
}
