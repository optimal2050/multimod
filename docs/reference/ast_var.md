# Create a variable AST node

Create a variable AST node

## Usage

``` r
ast_var(name, args = character())
```

## Arguments

- name:

  Name of the variable.

- args:

  Optional vector of dimension names (subscripts).

## Value

An `ast_var` S3 object (subclass of `multimod_ast`).
