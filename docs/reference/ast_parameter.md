# Create a parameter AST node

Create a parameter AST node

## Usage

``` r
ast_parameter(name, dims = ast_dims(), ...)
```

## Arguments

- name:

  Name of the parameter.

- dims:

  Optional vector of dimension `dims` objects.

## Value

An `parameter` S3 object (subclass of `ast`).
