# Create log entry for parameter/mapping link

Create log entry for parameter/mapping link

## Usage

``` r
create_link_log_entry(
  multimod_name,
  scenario_name,
  type,
  status,
  ert_param,
  inMemory
)
```
