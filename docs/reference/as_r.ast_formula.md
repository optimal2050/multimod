# Convert ast_formula to R Code

Convert ast_formula to R Code

## Usage

``` r
# S3 method for class 'ast_formula'
as_r(x, ...)
```

## Arguments

- x:

  ast_formula object

- ...:

  Additional arguments passed to as_r for the wrapped expression

## Value

R code string
