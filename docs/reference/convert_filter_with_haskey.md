# Convert filter expression with haskey checks for parameters (for sum filters)

Convert filter expression with haskey checks for parameters (for sum
filters)

## Usage

``` r
convert_filter_with_haskey(expr, model, ...)
```
