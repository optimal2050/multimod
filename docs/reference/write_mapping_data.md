# Write mapping data (tuples)

Write mapping data (tuples)

## Usage

``` r
write_mapping_data(mapping_obj, model = NULL)
```

## Arguments

- mapping_obj:

  Mapping object

- model:

  Model object (for lazy loading)

## Value

Character vector of GMPL data statements
