# Mark sets, parameters, and mappings with empty data

Mark sets, parameters, and mappings with empty data

## Usage

``` r
mark_empty_data(model, verbose = FALSE)
```

## Arguments

- model:

  A multimod model object

- verbose:

  Logical. Print progress messages

## Value

Model with `$trimmed = TRUE` on empty elements
