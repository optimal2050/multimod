# Convert an equation node to Pyomo rule definition (internal helper)

Convert an equation node to Pyomo rule definition (internal helper)

## Usage

``` r
render_pyomo_equation(eq, model = NULL)
```
