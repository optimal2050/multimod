# Create a constant AST node

Create a constant AST node

## Usage

``` r
ast_const(value)
```

## Arguments

- value:

  A numeric or character constant.

## Value

An `ast_const` S3 object (subclass of `multimod_ast`).
