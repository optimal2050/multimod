# Save all mappings to disk

Save all mappings to disk

## Usage

``` r
save_all_mappings(model, format = "parquet", keep_in_memory = FALSE)
```

## Arguments

- model:

  Multimod model

- format:

  Data format

- keep_in_memory:

  Keep data in memory after saving?

## Value

Modified model
