# Convert an object to a multimod structure

This is a generic function that converts an object to a
multimod-compatible format.

## Usage

``` r
as_multimod(x, ...)
```

## Arguments

- x:

  An object to convert.

- ...:

  Additional arguments passed to methods.

## Value

A multimod object.
