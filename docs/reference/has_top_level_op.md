# Check if expression has operator at top level (outside parentheses/brackets)

Check if expression has operator at top level (outside
parentheses/brackets)

## Usage

``` r
has_top_level_op(expr, op_pattern)
```

## Arguments

- expr:

  Character string

- op_pattern:

  Regular expression pattern for operator

## Value

Logical
