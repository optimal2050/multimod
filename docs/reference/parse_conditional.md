# Parse conditional expression (if-then-else)

Parse conditional expression (if-then-else)

## Usage

``` r
parse_conditional(expr, symbols, depth, max_depth)
```

## Arguments

- expr:

  Character string

- symbols:

  Symbol table

- depth:

  Current depth

- max_depth:

  Maximum depth

## Value

AST node or NULL
