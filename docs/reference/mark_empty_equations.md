# Mark equations with empty domains or all variables trimmed

Mark equations with empty domains or all variables trimmed

## Usage

``` r
mark_empty_equations(model, verbose = FALSE)
```

## Arguments

- model:

  A multimod model object

- verbose:

  Logical. Print progress messages

## Value

Model with `$trimmed = TRUE` on equations with empty domains
