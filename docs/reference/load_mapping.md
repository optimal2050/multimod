# Load mapping data into memory

Load mapping data into memory

## Usage

``` r
load_mapping(model, mapping_name)
```

## Arguments

- model:

  Multimod model

- mapping_name:

  Name of mapping to load

## Value

Modified model with mapping data loaded
