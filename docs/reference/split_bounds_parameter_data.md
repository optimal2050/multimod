# Split bounds-style parameter into low/high tables

Split bounds-style parameter into low/high tables

## Usage

``` r
split_bounds_parameter_data(ert_param)
```

## Arguments

- ert_param:

  energyRt parameter object of type "bounds"

## Value

List with lo/up data.frames (each containing dims + value)
