# Format set elements for LaTeX display

Format set elements for LaTeX display

## Usage

``` r
format_set_elements(
  set_data,
  max_inline = 10,
  head_tail_threshold = 100,
  head_n = 3,
  tail_n = 3
)
```
