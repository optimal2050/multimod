# Collect scenario parameter data into memory

Collect scenario parameter data into memory

## Usage

``` r
collect_scenario_parameter_data(ert_param)
```

## Arguments

- ert_param:

  energyRt parameter object

## Value

data.frame with parameter values (may be empty)
