# Create a multimod equation object

Create a multimod equation object

## Usage

``` r
new_multimod_eqn(
  name,
  desc = NULL,
  dims,
  lhs,
  rhs,
  relation = "==",
  domain = NULL
)
```

## Arguments

- desc:
