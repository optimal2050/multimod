# Update model parameters with default values from energyRt

Update model parameters with default values from energyRt

## Usage

``` r
populate_defvals_from_energyrt(model)
```

## Arguments

- model:

  multimod model object

## Value

Updated model with defVal populated
