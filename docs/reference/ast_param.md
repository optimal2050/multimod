# Create a parameter AST node

Create a parameter AST node

## Usage

``` r
ast_param(name, args = character())
```

## Arguments

- name:

  Name of the parameter.

- args:

  Optional vector of dimension names (subscripts).

## Value

An `ast_param` S3 object (subclass of `multimod_ast`).
