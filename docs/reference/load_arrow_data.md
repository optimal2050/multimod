# Load data from disk using Arrow

Load data from disk using Arrow

## Usage

``` r
load_arrow_data(path, format = NULL, collect = TRUE)
```

## Arguments

- path:

  Path to data directory or file

- format:

  Optional format specification

- collect:

  Logical. If TRUE, collect into memory (data.table)

## Value

Arrow dataset or data.table
