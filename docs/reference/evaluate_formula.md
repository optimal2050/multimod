# Evaluate formula AST over all dimension combinations

Evaluate formula AST over all dimension combinations

## Usage

``` r
evaluate_formula(
  formula_ast,
  dims,
  dims_index_aliases,
  all_data,
  model,
  verbose = FALSE
)
```
