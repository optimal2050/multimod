# Create a constant AST node

Create a constant AST node

## Usage

``` r
ast_constant(value, ...)
```

## Arguments

- value:

  A numeric or character constant.

## Value

An `constant` S3 object (subclass of `ast`).
