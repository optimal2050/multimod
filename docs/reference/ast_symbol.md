# Create a symbol AST node (for unclassified identifiers)

Create a symbol AST node (for unclassified identifiers)

## Usage

``` r
ast_symbol(name, ...)
```

## Arguments

- name:

  A character string representing the symbol.

## Value

An `symbol` S3 object (subclass of `ast`).
