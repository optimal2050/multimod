# Get data path for a parameter or mapping

Get data path for a parameter or mapping

## Usage

``` r
get_data_path(obj, base_path = NULL)
```

## Arguments

- obj:

  Parameter or mapping object

- base_path:

  Optional base path to prepend for relative paths

## Value

Character path to data directory
