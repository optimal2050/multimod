# Parse indexed access (array/parameter subscript)

Parse indexed access (array/parameter subscript)

## Usage

``` r
parse_indexed_access(expr, symbols, depth, max_depth)
```

## Arguments

- expr:

  Character string

- symbols:

  Symbol table

- depth:

  Current depth

- max_depth:

  Maximum depth

## Value

AST node or NULL
